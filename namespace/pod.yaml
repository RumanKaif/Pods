---
apiVersion: v1
kind: Pod 
metadata:
  name: alpine-Pod
  namespace: default
spec:
  containers:
    - name: alpine-Pod
      image: alpine 
      ports:
        - containerPort: 8000
      args:
        - sleep
        - 30s